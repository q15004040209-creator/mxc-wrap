[package]
name = "mxc-rust-demo"
version = "0.1.0"
edition = "2021"
description = "MXC Rust sandbox wrapper demo"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true